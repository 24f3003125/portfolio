name: Email Verification Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f3003125@ds.study.iitm.ac.in
        run: echo "Hello, world!"
